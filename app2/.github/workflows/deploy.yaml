name: Deploy App2

on:
  push:
    paths:
      - 'app2/**' # Trigger only if files in app1 change

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Display message
        run: echo "Deploy for App2 ran"